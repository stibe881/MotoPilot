# This project pins Expo SDK 54, whose react-dom peer range doesn't perfectly
# match the installed react. npm 7+ would abort install with ERESOLVE, so we
# allow legacy peer resolution for every local install (EAS sets the same via
# NPM_CONFIG_LEGACY_PEER_DEPS in eas.json).
legacy-peer-deps=true
